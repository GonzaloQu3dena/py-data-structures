from abstract_data_type import DataContainer, StackImplementation

def exercise_1_limited_container():
    """
    Exercise 1: ADT with Business Logic
    
    Objective:
    Create a `LimitedContainer` implementation that respects a maximum capacity.
    
    Logic:
    1. Define `LimitedContainer` inheriting from `DataContainer`.
    2. Add a `capacity` parameter to the `__init__` method.
    3. In `add(item)`, check if the current size is less than the capacity 
       before adding. If full, print a warning or do nothing.
    4. Implement `remove()` and `size()` using a standard list.
    """
    # TODO: Implement logic
    pass